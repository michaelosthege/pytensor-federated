name: aefenv
channels:
  - conda-forge
dependencies:
  - black
  - isort
  - nest-asyncio
  - numpy
  - pytest
  - pytest-cov
  - pip
  - psutil
  - pip:
    - betterproto[compiler]==2.0.0b5
    - pymc==5.8.0
